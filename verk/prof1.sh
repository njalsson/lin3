#!/usr/bin/env bash
echo "there is a command that shows list of every user logged in, users"
echo "than you can pipe that into wc, which is a word counting program."

echo "so users | wc -w outputs"
echo users | wc -w
til eru adrar adferdir til
1
